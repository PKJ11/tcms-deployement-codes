1. Confirm .env actually has the IP values and restart

Get-Content "C:\Users\Administrator\Desktop\tcms-qbd-main\.env" | Select-String "NEXTAUTH_URL|MINIO_ENDPOINT"
Should show 192.168.1.167 in both. If it does:
nssm restart TCMS
Get-Service TCMS

2. Test from a second laptop on the same Wi-Fi

http://192.168.1.167:3000
Log in as 001 / QBD@1234, then upload a file and view/download it back — that last step is what actually proves MinIO is reachable, not just the login page.

3. Don't skip the power settings from last message

If you haven't run the powercfg block yet, do that now too — otherwise the first time someone closes this laptop's lid, everyone loses access without warning.

Paste back what happens on the second-laptop test.
