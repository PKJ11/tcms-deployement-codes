# Computer name
$env:COMPUTERNAME

# Is it domain-joined, and to what domain?
Get-WmiObject Win32_ComputerSystem | Select-Object Name, Domain, PartOfDomain

# Full DNS/FQDN name
[System.Net.Dns]::GetHostByName($env:COMPUTERNAME).HostName

# IP address(es)
ipconfig | findstr /i "IPv4"
